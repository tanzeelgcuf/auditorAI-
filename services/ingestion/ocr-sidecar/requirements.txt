# services/ingestion/ocr-sidecar/requirements.txt
#
# THE DISTRIBUTION NAME IS `python-doctr`, NOT `doctr`. Found 2026-09-06 while
# sweeping for other pins in the same never-published class as
# `langgraph==0.1.0`; this line was worse than that one. `doctr` on PyPI is an
# unrelated project — a docs-deploy tool for Travis/GitHub Pages, currently at
# 1.3.2 — which has no 0.8.x release and no `torch` extra. So
# `doctr[torch]==0.8.1` cannot resolve, and if it ever did resolve it would
# install a docs tool, after which main.py's `from doctr.io import DocumentFile`
# would fail at import: the `doctr.*` namespace this service uses is provided by
# python-doctr (docTR, by Mindee, now maintained by t2k).
#
# 0.8.1 is a real python-doctr release (changelog: v0.8.1, 2024-03-04) and is
# kept as-is so this is a one-line name correction and nothing else. Not
# installed here — no PyPI access in the environment this was fixed in.
python-doctr[torch]==0.8.1
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6
boto3==1.34.0